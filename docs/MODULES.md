# AzamiOS Modules & Architecture

This document is automatically generated by `scripts/autodoc.py`.

## `arch/x86_64/boot/`

> Boot protocol wrappers and early architecture initialization.

- **`limine_req.c`**: Limine Request Storage
- **`limine_req.h`**: Limine Boot Protocol Wrapper

## `arch/x86_64/cpu/`

> CPU-specific abstractions: GDT, IDT, LAPIC, SMP (Symmetric Multiprocessing), and interrupts.

- **`gdt.c`**: GDT / TSS Implementation (x86_64)
- **`gdt.h`**: Global Descriptor Table & Task State Segment (x86_64)
- **`idt.c`**: IDT Initialisation & Exception Dispatch
- **`idt.h`**: Interrupt Descriptor Table (x86_64)
- **`lapic.c`**: Local APIC Driver Implementation
- **`lapic.h`**: Local APIC Driver (xAPIC / x2APIC)
- **`msr.h`**: MSR & CPUID Helpers (x86_64)
- **`pic.c`**: Legacy 8259 PIC Implementation
- **`pic.h`**: Legacy PIC (8259) Driver
- **`smp.c`**: Symmetric Multiprocessing (SMP) Implementation
- **`smp.h`**: Symmetric Multiprocessing (SMP) Subsystem Header
- **`spinlock.h`**: Spinlock (x86_64, ticket-based for fairness)

## `arch/x86_64/mm/`

> Architecture-specific memory management, primarily the 4-level paging Virtual Memory Manager (VMM).

- **`vmm.c`**: 4-Level VMM Implementation (x86_64)
- **`vmm.h`**: Virtual Memory Manager (x86_64 4-level paging)

## `drivers/acpi/`

- **`acpi.c`**: ACPI Subsystem
- **`acpi.h`**: ACPI Subsystem
- **`ioapic.c`**: IO APIC Driver
- **`ioapic.h`**: IO APIC Driver
- **`power.c`**: Power Management & ACPI System Control Driver
- **`power.h`**: Power Management & ACPI System Control Driver Header

## `drivers/block/`

- **`ahci.h`**: AHCI Controller Definitions
- **`ata.c`**: ATA / IDE PIO Block Device Driver (ata.c)
- **`ata.h`**: ATA / IDE PIO Block Device Driver (ata.h)
- **`block.c`**: Block Device Abstraction & Ramdisk/AHCI Implementation
- **`block.h`**: Block Device Abstraction Header
- **`virtio_blk.c`**: VirtIO Block Device Driver Implementation
- **`virtio_blk.h`**: VirtIO Block Device Driver Header

## `drivers/char/`

- **`console.c`**: Unified Console Implementation
- **`console.h`**: Unified Console (UART + optional framebuffer)
- **`console_font.h`**: Standard 8x16 IBM VGA Bitmap Font (ASCII 0x20..0x7E)
- **`lpt.c`**: Parallel Port (LPT) Interrupt-Driven Driver
- **`lpt.h`**: Parallel Port (LPT) Driver
- **`memdevs.c`**: Standard Memory Devices Driver (/dev/null, /dev/zero, /dev/random, /dev/kmsg, /dev/console)
- **`memdevs.h`**: Standard Memory Devices Driver Header (/dev/null, /dev/zero, etc.)
- **`uart.c`**: UART (16550) Interrupt-Driven Driver
- **`uart.h`**: UART (16550) Early Console Driver

## `drivers/input/`

- **`input.c`**: Input Subsystem Implementation (PS/2 Keyboard + Mouse)
- **`input.h`**: Input Subsystem (PS/2 Keyboard + Mouse)

## `drivers/misc/`

- **`bga.c`**: Bochs Graphics Adapter (BGA) Driver
- **`bga.h`**: Bochs Graphics Adapter (BGA) Driver
- **`rtc.c`**: Real Time Clock (RTC) Driver
- **`rtc.h`**: Real Time Clock (RTC) Driver Header

## `drivers/net/`

- **`e1000.c`**: Intel 8254x / 82574L (e1000 / e1000e) Gigabit Ethernet Driver
- **`e1000.h`**: Intel 8254x / 82574L (e1000 / e1000e) Gigabit Ethernet Driver Header
- **`rtl8139.c`**: Realtek RTL8139 Fast Ethernet NIC Driver
- **`rtl8139.h`**: Realtek RTL8139 Fast Ethernet NIC Driver Header
- **`virtio_net.c`**: VirtIO Network Device Driver Implementation
- **`virtio_net.h`**: VirtIO Network Device Driver Header

## `drivers/sound/`

- **`ac97.c`**: Intel AC97 Audio Driver Implementation
- **`ac97.h`**: Intel AC97 Audio Driver Header
- **`pcspeaker.c`**: PC Speaker (PIT Channel 2) Sound Driver
- **`pcspeaker.h`**: PC Speaker (PIT Channel 2) Sound Driver Header
- **`sound.c`**: Sound Subsystem Core Implementation
- **`sound.h`**: Sound Subsystem Core Header

## `drivers/video/`

- **`fbdev.c`**: POSIX Framebuffer Device (fbdev)
- **`virtio_gpu.c`**: VirtIO-GPU Driver Implementation
- **`virtio_gpu.h`**: VirtIO-GPU Driver Header
- **`virtio_gpu_cmd.c`**: VirtIO-GPU Driver Commands

## `kernel/`

> Core microkernel entry points and essential system headers.

- **`main.c`**: Kernel Main Entry Point
- **`panic.c`**: Kernel Panic
- **`panic.h`**: Kernel Panic Header
- **`uaccess.h`**: User Space Memory Access

## `kernel/ipc/`

> Inter-Process Communication mechanisms (messaging and shared memory).

- **`ipc.c`**: Inter-Process Communication (IPC) Implementation
- **`ipc.h`**: Inter-Process Communication (IPC) Header

## `kernel/lib/`

> Kernel-internal freestanding C library functions (string manipulation, memory copying).

- **`string.c`**: Freestanding String & Memory Utility Implementation
- **`string.h`**: Freestanding String & Memory Utility Header

## `kernel/mm/`

> Hardware-independent memory management: Buddy allocator (PMM) and Slab/Bucket allocator (kmalloc).

- **`kmalloc.c`**: Kernel Memory Allocator Implementation (Bucket/Slab Allocator)
- **`kmalloc.h`**: Kernel Memory Allocator (kmalloc / kfree) Header
- **`pmm.c`**: Physical Memory Manager: Buddy Allocator
- **`pmm.h`**: Physical Memory Manager (Buddy Allocator)

## `kernel/net/`

- **`net.c`**: Kernel Network Stack (Ethernet, ARP, IPv4, ICMP)

## `kernel/object/`

> NT-Style Object Manager for unified resource naming and lifecycle management.

- **`object.c`**: NT-Style Object Manager Implementation
- **`object.h`**: NT-Style Object Manager Header

## `kernel/sched/`

> Completely Fair Scheduler (CFS), thread management, and 64-bit ELF executable loading.

- **`elf.c`**: 64-bit ELF Executable Loader & User Process Spawner
- **`elf.h`**: 64-bit ELF Executable Loader Header
- **`sched.c`**: CFS Scheduler & Process/Thread Management Implementation
- **`sched.h`**: CFS Scheduler & Process/Thread Management Header

## `kernel/security/`

> Security hardening features such as stack canaries and execution protection.

- **`security.c`**: Security Hardening Implementation
- **`security.h`**: Security Hardening & Canary Subsystem Header

## `kernel/syscall/`

> System call dispatcher handling requests from user space.

- **`syscall.c`**: System Call Dispatcher Implementation
- **`syscall.h`**: System Call Dispatcher

## `userland/apps/about/`

- **`main.c`**: About AzamiOS

## `userland/apps/azwm/`

> AzamiOS Window Manager (Compositor and Desktop shell).

- **`compositor.c`**: Display Server Compositor Implementation
- **`compositor.h`**: Display Server Compositor
- **`de_compositor.h`**: DE Compositor Extension Header
- **`de_protocol.h`**: Desktop Environment Extended Protocol
- **`desktop.c`**: Desktop Rendering Implementation
- **`desktop.h`**: Desktop Rendering (Background, Taskbar, Cursor, Font)
- **`main.c`**: Display Server (azwm) Main Event Loop   v2.0
- **`protocol.h`**: Display Server Protocol Definition

## `userland/apps/calculator/`

- **`main.c`**: Calculator

## `userland/apps/clock/`

- **`main.c`**: Clock

## `userland/apps/fetch/`

- **`main.c`**: System Information Fetch Utility (fetch.elf)

## `userland/apps/gui_test/`

- **`main.c`**: GUI Client Test Application

## `userland/apps/settings/`

- **`main.c`**: Settings Panel (v2.0 with Audio Controls & Telemetry)

## `userland/apps/sysmon/`

- **`main.c`**: System Monitor

## `userland/apps/terminal/`

- **`main.c`**: Terminal Emulator (v3.0)

## `userland/apps/texteditor/`

- **`main.c`**: Text Editor

