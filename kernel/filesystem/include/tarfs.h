/**
 * kernel/filesystem/include/tarfs.h — Kernel tarfs header bridge
 *
 * The canonical definitions live in lib/fs/tarfs.h.
 * Re-exported here so existing kernel includes are unaffected.
 */
#include "../../../lib/fs/tarfs.h"