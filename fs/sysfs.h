/* ============================================================================
 * AzamiOS — System Virtual Filesystem (sysfs) Header
 * File: fs/sysfs.h
 * ============================================================================ */
#pragma once

#include "vfs.h"

/** sysfs_init() — Register the sysfs filesystem type in VFS. */
void sysfs_init(void);
